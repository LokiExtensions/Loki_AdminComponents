<script>
    const LokiAdminFiltersComponentPartial = {
        showFilters: true,
        toggleFilters() {
            this.showFilters = !this.showFilters;
        },
        filterButtonClass() {
            return this.showFilters ? '_active' : '';
        },
    }
</script>
